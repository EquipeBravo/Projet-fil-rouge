{% extends 'AppBundle:admin:layout/layout.html.twig' %}
{% block body %}
    <h1>Lieu</h1>

    <table>
        <tbody>
            <tr>
                <th>Salle</th>
                <td>{{ place.room }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ place.city }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('place_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('place_edit', { 'id': place.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
