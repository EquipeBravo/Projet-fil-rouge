{% extends 'AppBundle:admin:layout/layout.html.twig' %}

{% block body %}
    <h1>Nouveau lieu</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <input type="submit" value="Valider" class="btn btn-sm btn-success"/>
        </div>
    </div>
    {{ form_end(form) }}

    <ul class="list-inline">
        <li>
            <a href="{{ path('place_index') }}" class="btn btn-sm btn-default">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
