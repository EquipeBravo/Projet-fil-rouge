{% extends 'AppBundle::base.html.twig' %}

{% block body %}

    <h1>Inscription</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <p class="col-sm-offset-2 col-sm-10">
        * Votre mot de passe doit contenir une minuscule, une majuscule, un caractère spécial '@$*#!_',
        un chiffre et doit comporter au moins 8 caractères.
    </p>
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <input type="submit" value="Valider" class="btn btn-sm btn-success"/>
        </div>
    </div>
    {{ form_end(form) }}

{% endblock %}