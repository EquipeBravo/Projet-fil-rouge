{% extends 'AppBundle:Layout:layout.html.twig' %}

{% block body %}
    <h1>Modifiez votre compte</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <input type="submit" value="Modifier" class="btn btn-sm btn-warning"/>
            </div>
        </div>
    {{ form_end(edit_form) }}

    <ul class="list-inline">
        <li>
            <a href="{{ path('person_index') }}" class="btn btn-sm btn-default">Retour à la liste</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer" class="btn btn-sm btn-danger">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
