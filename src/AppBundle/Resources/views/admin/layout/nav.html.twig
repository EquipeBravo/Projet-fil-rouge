<nav class="navbar navbar-inverse">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>²
            </button>
            <a title="Retour au site" class="navbar-brand" href="{{ path('app_homepage') }}"><span
                        id="logo">ASPTT</span></a>
        </div>
        <div class="collapse navbar-collapse" id="navbar">
            <ul class="nav navbar-nav navbar-left menu">
                <li title="Accueil Admin"><a href="{{ path('app_admin_dashboard') }}"><span
                                class="glyphicon glyphicon-home"></span></a></li>
                <li class="dropdown">
                    <a href="{{ path('person_index') }}" class="dropdown-toggle" data-toggle="dropdown">
                        Membres
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('person_index') }}">Tous</a></li>
                        {% for role in roles %}
                            <li><a href="{{ path('role_person_homepage', {'id': role.id}) }}">{{ role.roleName  }}</a></li>
                        {% endfor %}
                    </ul>
                </li>
                <li><a href="{{ path('app_admin_club') }}">A propos
                </a></li>
                <li><a href="{{ path('app_admin_dashboard') }}gallery">Galerie</a></li>
                <li><a href="{{ path('category_index') }}">Categories</a></li>
                <li class="dropdown">
                    <a href="{{ path('team_index') }}" class="dropdown-toggle" data-toggle="dropdown">
                        Équipes
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('team_index') }}">Toutes</a></li>
                        {% for category in categories %}
                            <li><a href="{{ path('category_team_homepage', {'id': category.id}) }}">{{ category.description  }}</a></li>
                        {% endfor %}
                    </ul>
                </li>
                <li><a href="{{ path('event_index') }}">Évènements</a></li>
                <li class="dropdown">
                    <a href="{{ path('matchs_index') }}" class="dropdown-toggle" data-toggle="dropdown">
                        Matchs
                        <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('matchs_index') }}">Matchs</a></li>
                        <li><a href="{{ path('place_index') }}">Salles</a></li>
                    </ul>
                </li>
                <!-- manque école d'arbitrage et contact
                le lien pour s'inscrire peut éventuellement être déporté à droite du menu et disparaitre si la personne est logguée-->
            </ul>
            <ul class="nav navbar-nav navbar-right list-inline">
                <li title="Logout"><a href="{{ path('app_logout') }}">
                        <span class="glyphicon glyphicon-log-out"></span> Déconnexion</a>
                </li>
            </ul>
        </div>
    </div>
</nav>